[pytest]
addopts = -m "not live and not network and not paid"
markers =
    live: calls a live model provider
    network: requires outbound network access
    paid: may consume paid credits or subscription quota
